setup:
  config:
    env_file: .env

build:
  docker:
    app: ./app/Dockerfile
    api: ./api/Dockerfile

run:
  app: bash  # streamlit run --browser.serverAddress app-env --browser.serverPort ${PORT} --server.port ${PORT} --theme.base dark app.py
  api: bash  # python api.py